@model IEnumerable<BookCarProjectMaster.Models.CarProduct>

@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
@foreach (var item in Model)
{
    <div class="col-lg-4 col-md-6 con suv mpv">
        <div class="single-popular-car">
            <div class="p-car-thumbnails">
                <a class="car-hover" href="@item.ImageFont">
                    <img src="@item.ImageFont" alt="@item.ImageFont">
                </a>
            </div>

            <div class="p-car-content">
                <h3>
                    <a href="@Url.Action("Details", "Home", new { id = item.CarProductsId })">@item.ModelCar</a>
                    <span class="price"><i class="fa fa-tag"></i> @item.RentCost$/day</span>
                </h3>

                <h5>Info</h5>

                <div class="p-car-feature">
                    <a>Màu: @item.CarColor</a>
                    <a>Số Ghế: @item.NumberOfSeats</a>
                    @Html.ActionLink("Chi Tiết", "Details", new { id = item.CarProductsId })
                </div>
            </div>
        </div>
    </div>
}


